#!/usr/bin/env node
/**
 * Legacy helper — use `npm run audit-replays` for the full link audit.
 *
 * After auditing, open the dev report page:
 *   npm run dev
 *   http://localhost:3000/dev/replay-audit
 */

console.log("Use: npm run audit-replays");
console.log("Then open: http://localhost:3000/dev/replay-audit");
